from django.apps import AppConfig


class AccountConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "core_apps.account"

    verbose_name = "core_apps.account"
    verbose_name_plural = "core_apps.account"
